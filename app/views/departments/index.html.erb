<ul>
  <% @departments.each do |user| %>
    <li>
      <%= user.name %>

      <%= link_to "Show", department_path(user) %>
      <%= link_to 'Edit', edit_user_path(user) %>
      <%= button_to 'Delete', user_path(user), :method => :delete, :data => { :confirm => "Are you sure?" } %>
    </li>
  <% end %>
</ul>
  
  <%= link_to 'New User', new_department_path %>